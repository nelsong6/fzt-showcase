{
  "name": "fzt-showcase",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "build:wasm": "cd ../../../fzt && GOOS=js GOARCH=wasm go build -o ../fzt-showcase/frontend/fzt.wasm ./cmd/wasm",
    "test": "echo \"No tests\" && exit 0",
    "lint": "echo \"No linter\" && exit 0"
  }
}
